# Simple Password Generator with Node.js

Clone repository
```
git clone https://github.com/sideka-cloud/password-generator.git
cd password-generator
```

Run with docker
```
docker-compose up --build -d
```

Access with url : `http://IP:3000`

Remove container
```
docker compose down -v
```
